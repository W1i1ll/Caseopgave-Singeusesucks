---
import bg1 from "../assets/images/section2.png"; // Background for section 1
import { Image } from "astro:assets";
---

<section class="w-full flex flex-col items-center">
  <!-- Section 1: Mobilisering og engagement -->
  <div class="relative min-h-screen flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 -z-10">
      <Image src={bg1} alt="Background 1" class="object-cover w-full h-full" />
    </div>
    <div class="relative z-10 text-center bg-blue-800 bg-opacity-80 p-6 rounded-md max-w-md text-white">
      <h1 class="text-xl font-bold mb-4">Sådan arbejder vi for en bedre fremtid</h1>
      <h2 class="text-lg font-semibold mb-2">Mobilisering og engagement</h2>
      <p class="text-sm">
        Vi mobiliserer og engagerer befolkningen gennem folkeoplysning om plastikforureningens alvorlige konsekvenser og de løsninger, vi mener, kan løse problemet...
      </p>
    </div>
  </div>

<script client:load>
  import { animate, inView } from "motion";

  document.addEventListener("DOMContentLoaded", () => {
    // Add fade-in effect for each section's text overlay when it comes into view
    document.querySelectorAll('.bg-blue-800').forEach((element) => {
      inView(element, () => {
        animate(element, { opacity: 1, transform: "translateY(0)" });
      }, { rootMargin: "-50px 0px" });
    });
  });
</script>
