---
import bg1 from "../assets/images/section1.jpg"; // First section background
import bg2 from "../assets/images/section2.png"; // Second section background
import bg3 from "../assets/images/section3.png"; // Third section background
import { Image } from "astro:assets";
---

<section class="w-full flex flex-col items-center">
  <!-- Section 1 -->
  <div class="relative h-screen w-screen flex flex-col items-center justify-center overflow-hidden">
    <div class="absolute inset-0 z-0">
      <Image src={bg1} alt="Background 1" class="object-cover w-screen h-screen" />
    </div>
    <div class="relative z-10 text-center bg-blue-800 bg-opacity-80 p-6 rounded-md max-w-md text-white sticky top-20">
      <h1 class="text-xl font-bold mb-4">Sådan arbejder vi for en bedre fremtid</h1>
      <h2 class="text-lg font-semibold mb-2">Mobilisering og engagement</h2>
      <p class="text-sm">
        Vi mobiliserer og engagerer befolkningen gennem folkeoplysning om plastikforureningens alvorlige konsekvenser og de løsninger, vi mener, kan løse problemet...
      </p>
    </div>
  </div>

  <!-- Section 2 -->
  <div class="relative h-screen w-screen flex flex-col items-center justify-center overflow-hidden">
    <div class="absolute inset-0 z-0">
      <Image src={bg2} alt="Background 2" class="object-cover w-screen h-screen" />
    </div>
    <div class="relative z-10 text-center bg-blue-800 bg-opacity-80 p-6 rounded-md max-w-md text-white sticky top-20">
      <h2 class="text-lg font-semibold mb-2">Politik og grøn lovgivning</h2>
      <p class="text-sm">
        Vi samarbejder med industripartnere om konkrete løsninger og fremmer industriel innovation for at tackle plastikforureningen...
      </p>
    </div>
  </div>

  <!-- Section 3 -->
  <div class="relative h-screen w-screen flex flex-col items-center justify-center overflow-hidden">
    <div class="absolute inset-0 z-0">
      <Image src={bg3} alt="Background 3" class="object-cover w-screen h-screen" />
    </div>
    <div class="relative z-10 text-center bg-blue-800 bg-opacity-80 p-6 rounded-md max-w-md text-white sticky top-20">
      <h2 class="text-lg font-semibold mb-2">Politik og grøn lovgivning</h2>
      <p class="text-sm">
        Vi arbejder aktivt med politik og grøn lovgivning for at skabe ambitiøse nationale mål, der skal reducere plastikforurening og øge genbrug af plastik...
      </p>
    </div>
  </div>
</section>


<script>
  import { animate, inView } from "motion";

  document.addEventListener("DOMContentLoaded", () => {
    // Add fade-in effect for each section when it comes into view
    document.querySelectorAll('.sticky').forEach((element, index) => {
      inView(element, () => {
        animate(element, { opacity: 1, transform: "translateY(0)" });
      }, { rootMargin: "-50px 0px" });
    });
  });
</script>
